export const renderSource = (index: number) => {
  switch (index) {
    case 1:
      return 'website';

    case 2:
      return 'app';

    case 3:
      return 'bgst';

    default:
      return 'website';
  }
};
